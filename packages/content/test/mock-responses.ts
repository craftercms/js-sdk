import { item,
         descriptor,
         children,
         tree,
         navTree,
         navBreadcrumb } from '../../../util/mock-responses-common';

export { item, descriptor, children, tree, navTree, navBreadcrumb };

export const renderUrl = '/style';

export const storeUrl = '/site/website/technology/index.xml';